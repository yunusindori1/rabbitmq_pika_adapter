name: Release (deprecated)

on:
  workflow_dispatch:

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "This workflow is deprecated. Publishing is handled by .github/workflows/publish.yml on pushes to develop (TestPyPI) and release (PyPI)."
